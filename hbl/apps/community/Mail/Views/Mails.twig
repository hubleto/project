<h1 class="app-main-title">{{ translate('Mails') }}</h1>

<div class="flex gap-2 w-full">
  {% include '@HubletoApp:Community:Mail/FolderSelector.twig' %}
  <div>
    <div class="badge badge-extra-large ml-0">Inbox</div>
    <div class="mt-4">
      <app-mail-list-folder string:folder="inbox"></app-mail-list-folder>
    </div>
  </div>
</div>